{
  "name": "@vision-web-workspace/mac-builder-agent",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vision-web-workspace/contracts": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
